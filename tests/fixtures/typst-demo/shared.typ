// Minimal "shared template" fixture -- exists so
// tests/backends-typst.test.sh can verify content-hash.sh follows a
// local #import (real courses share a template file across many
// sources; this fixture stands in for that pattern with the smallest
// possible file).
#let banner = "Demo Studio"
