# latex-beamer backend

Placeholder. The LaTeX/Beamer renderer backend (Stage 3 of the extraction
from cs1101s/course-materials) lands here: `build-lecture.sh`,
`build-studio.sh`, the shared preamble/theme/macro files (today's
`src/common/`), and the LaTeX-specific implementations of the
[renderer backend contract](../../README.md#renderer-backend-contract)
(`build_slot`, `content_hash`, `extract_title`, `extract_date`).

Not yet implemented.
